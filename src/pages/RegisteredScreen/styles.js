import { StyleSheet } from 'react-native';
import { general } from '../../styles';
import registerStyle from '../RegisterScreen/styles';

const styles = StyleSheet.create({
  ...general, 
  ...registerStyle
});

export default styles;
